{% extends 'base.html.twig' %}

{% block body %}

    <div class="col-md-6">
        <h1>Nouvel auteur</h1>

        {{ form_start(authorForm) }}
        <div class="row">
            <div class="col-md-6">
                {{ form_row(authorForm.name) }}
            </div>
            <div class="col-md-6">
                {{ form_row(authorForm.firstName) }}
            </div>
            <div class="col-md-6">
                {{ form_row(authorForm.country) }}
            </div>
            <div class="col-md-6 align-self-end">
                {{ form_row(authorForm.submit) }}
            </div>
        </div>
        {{ form_end(authorForm) }}
    </div>


{% endblock %}

{% block title %}

{% endblock %}

